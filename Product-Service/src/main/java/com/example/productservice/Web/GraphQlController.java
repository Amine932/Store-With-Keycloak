package com.example.productservice.Web;
/*

import com.example.productservice.DTOs.ProductRequestDTO;
import com.example.productservice.DTOs.ProductResponseDTO;
import com.example.productservice.Entities.Product;
import com.example.productservice.Service.ProductServiceInterface;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.graphql.data.method.annotation.Argument;
import org.springframework.graphql.data.method.annotation.MutationMapping;
import org.springframework.graphql.data.method.annotation.QueryMapping;
import org.springframework.stereotype.Controller;

import java.util.List;


@Controller
public class GraphQlController {

    @Autowired
    ProductServiceInterface productServiceInterface;


    @QueryMapping
    public List<ProductResponseDTO> listProducts()
    {
        return productServiceInterface.listProducts();
    }
    @QueryMapping
    public ProductResponseDTO ProductById(@Argument Integer id)
    {
        return productServiceInterface.productById(id);

    }

    @MutationMapping
    public Product addProduct(@Argument ProductRequestDTO productRequestDTO)
    {
        return productServiceInterface.addProduct(productRequestDTO);
    }

    @MutationMapping
    public ProductResponseDTO  updateProduct(@Argument Integer id,@Argument ProductRequestDTO productRequestDTO)
    {
         productServiceInterface.updateProduct(id,productRequestDTO);

        return productServiceInterface.productById(id);
    }

    @MutationMapping
    public void deleteProduct(@Argument Integer id)
    {
        productServiceInterface.deleteProduct(id);
    }

}

*/
